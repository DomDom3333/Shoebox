<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@(ViewData["Title"] is string t && t.Length > 0 ? $"{t} · GroupPhoto" : "GroupPhoto")</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header class="site-header">
        <a class="brand" href="/">Group<em>Photo</em></a>
        @RenderSection("HeaderActions", required: false)
    </header>
    <main class="container">
        @RenderBody()
    </main>
    <footer class="site-footer">
        <span>GroupPhoto — shared photo pools, no accounts needed.</span>
        <span>Made for one weekend, not forever. Download what you love.</span>
    </footer>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
